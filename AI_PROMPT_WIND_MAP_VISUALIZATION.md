基于谷歌底图的风场可视化Web应用开发提示词

请开发一个支持地图缩放的风场可视化Web应用，具体需求如下：

**核心功能要求**：
- 使用Google Maps或Mapbox作为底图，支持多层级地图缩放（zoom level 0-18）
- 实现风场粒子可视化系统，粒子颜色根据风速大小动态映射（蓝色=微风，红色=强风）
- 支持鼠标交互：缩放、平移、点击查看风场数据详情
- 响应式设计，适配桌面和移动端

**技术栈选择**：
- 前端框架：React + TypeScript
- 地图服务：Google Maps JavaScript API 或 Mapbox GL JS
- 数据格式：GeoJSON + 自定义风场数据格式
- 渲染引擎：WebGL + Three.js 或 D3.js + Canvas
- 构建工具：Vite

**数据处理需求**：
- 支持风场数据格式：NetCDF
- 数据金字塔实现：根据缩放级别动态加载不同精度的风场数据
- 实时数据更新：支持WebSocket推送新数据
- 数据压缩：使用二进制格式减少网络传输

**可视化设计**：
- 粒子系统：每个粒子代表一个风场网格点，粒子轨迹动态更新
- 颜色映射：实现自适应颜色映射，根据当前视图范围内的风速分布动态调整颜色渐变
- 动画效果：粒子移动轨迹使用残影效果，显示风的流向
- 图例控制：提供风速图例、图层开关、时间滑块等UI控件

**地图集成要求**：
- 地理配准：确保风场数据与地图地理坐标精确对应
- 多尺度适配：在不同缩放级别下自动调整粒子密度和渲染精度

**性能优化策略**：
- LOD(Level of Detail)：根据地图缩放级别动态调整粒子数量和粒子大小
- 视锥体裁剪：只渲染当前视野范围内的粒子
- 纹理优化：使用纹理图集减少WebGL状态切换
- 内存管理：实现粒子对象的池化机制，避免频繁创建销毁

**用户交互功能**：
- 地图缩放：支持鼠标滚轮、触摸手势缩放
- 平移拖拽：平滑的地图平移动画
- 数据查询：点击任意位置显示该点的风速、风向、温度等数据
- 时间控制：时间滑块控制显示不同时间点的风场数据
- 图层管理：可开关风场图层、底图图层、等值线图层

**数据可视化细节**：
- 风向显示：在粒子旁边添加风向箭头或线条
- 风速分级：提供多种风速分级方案（蒲福风级、m/s、km/h）
- 等值线：可选显示风速等值线，线宽和颜色根据风速变化
- 统计信息：显示当前视图范围内的风速统计（最大值、最小值、平均值）

**部署和扩展性**：
- 模块化设计：风场渲染、地图控制、数据管理分离
- 插件系统：支持第三方扩展，如添加温度、湿度等其他气象要素
- API设计：提供RESTful API供其他系统调用
- 国际化支持：多语言界面

**兼容性要求**：
- 现代浏览器支持：Chrome 80+, Firefox 75+, Safari 13+, Edge 80+
- 移动端适配：响应式设计，支持触摸操作
- 性能基准：在1920x1080分辨率下，50,000粒子时保持60fps

请基于以上需求生成完整的实现代码，包括前端页面、地图集成、风场渲染、数据处理等所有模块，确保代码结构清晰、性能优良、用户体验良好。